<template><aside><button class="new" @click="chat.newChat()">＋ 新会话</button><div class="items"><button v-for="x in list" :key="x.id" class="item" :class="{active:currentId===x.id}" @click="open(x.id)">{{x.title}}</button></div></aside></template>
<script setup lang="ts">import { storeToRefs } from 'pinia';import { useSessionsStore } from '../stores/sessions';import { useChatStore } from '../stores/chat';const s=useSessionsStore(),chat=useChatStore();const {list,currentId}=storeToRefs(s);function open(id:string){void chat.openSession(id)}</script>
<style scoped>aside{width:248px;border-right:1px solid var(--border);padding:14px 10px;flex-shrink:0}.new,.item{width:100%;border:0;border-radius:10px;cursor:pointer;text-align:left}.new{background:var(--pink);color:#fff;padding:10px 14px;text-align:center;margin-bottom:12px}.item{background:transparent;color:var(--text);padding:10px 12px;margin:2px 0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.item:hover{background:var(--soft)}.item.active{background:var(--pinksoft);color:var(--pink2)}@media(max-width:640px){aside{width:165px}}</style>
